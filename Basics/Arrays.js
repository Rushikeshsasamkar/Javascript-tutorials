let arr = [1,2,4,5,6]
// let newArr = arr.join()
// console.log(typeof newArr);

let newArr2 = arr.splice(1,3);
console.log(newArr2);
// splice make the changes in original array

let newArr1 = arr.slice(1,3);
console.log(newArr1);
// slice doesnt make the changes in the original array

