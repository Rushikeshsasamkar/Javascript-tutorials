const map = new Map();

// map.set = ('In','India')

const myObject = {
    name:'Rushikesh',
    userId:"abc123",
    password:"Pass@123"
}

// myObject.map((e)=>console.log(e))
// objects is not iteratable in that way


// for(const key in myObject){
//     console.log(`Key: ${key} and value is ${myObject[key]}`);
// }

// for (const key in object) {
//     if (Object.hasOwnProperty.call(object, key)) {
//         const element = object[key];
        
//     }
// }

// for in could also possible inside the array to get keys and values


// const coding = {
//     name:'Rushikesh',
//     userId:"abc123",
//     password:"Pass@123"
// };

// coding.forEach((element) => {
//     console.log(element);
// });

// forEach method is not able to iterate the objects it can only iterate the arrays